v0.7.0
* Added support for V2Ray
* Set target .NET Framework version to 4.7.2
* Optimized the server response time check operation
* Fixed a color problem in light theme

v0.6.0
* Update native components to the static link version, no need to install runtime library now
* Added auto-run feature, you can turn it on or off in settings
* Added light theme, you can switch the theme in settings
* Added checking server response time function to the server list
* Added support for separated information acquisition and detection to the server list
* Added active server indication to the server list 
* Added fetch information, checking response time, checking ping delay functions to the server profile editing.
* Added task prompt when closing the XCURL window

v0.5.5
* Added server port info in settings
* Added a "replace old server" option in settings, This option works when adding a server
* Added separate progress indicators and controls for background tasks
* Added stop action to the ping delay checking for IP data
* The main window visibility is now saved
* Adjusted some UI text
* Some optimize

v0.5.4
* Adjusted some UI text
* Rename the data folder from "datas" to "data"
* Fixed a problem caused by fetch server information
* Improved progress display
* Other stability improvements

v0.5.3
* Initial release
